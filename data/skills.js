/**
 * Skill groups, rendered as chips.
 * Source: extracted verbatim from app/page.js (Phase 0 audit).
 *
 * The original UI grouped related technologies into a single comma-separated
 * label per chip (no proficiency levels or category taxonomy existed).
 * That grouping is preserved as-is rather than invented/re-categorized.
 */
const skills = [
  "Java, Spring Boot",
  "Microservices, Spark",
  "AWS, Terraform, Spinnaker",
  "ReactJS, JavaScript",
  "SQL, Oracle, MySQL",
  "Docker, Kubernetes",
];

export default skills;
