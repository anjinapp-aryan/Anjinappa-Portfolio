/**
 * Engineering Labs (hands-on experiments / developer tooling).
 *
 * STATUS: no source content exists yet. This file intentionally exports
 * an empty array. Do not populate with invented experiments.
 *
 * Shape (for when real content is provided):
 * {
 *   slug: string,
 *   title: string,
 *   description: string,
 *   technologies: string[],
 *   github: string | null,
 *   demo: string | null,
 * }
 */
const labs = [];

export default labs;
