/**
 * Featured projects / case studies.
 *
 * STATUS: no source content exists yet (see docs/REUSE-AUDIT.md and
 * docs/ARCHITECTURE.md, Phase 0 findings). This file intentionally
 * exports an empty array. Do not populate with invented projects.
 *
 * Shape (for when real project data is provided):
 * {
 *   slug: string,
 *   title: string,
 *   description: string,
 *   categories: string[],
 *   technologies: string[],
 *   image: string | null,
 *   github: string | null,
 *   demo: string | null,
 *   featured: boolean,
 * }
 */
const projects = [];

export default projects;
